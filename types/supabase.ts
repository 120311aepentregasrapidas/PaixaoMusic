/**
 * ATENÇÃO: este arquivo é um placeholder.
 *
 * Depois de aplicar `supabase/migrations/0001_init.sql` no projeto Supabase,
 * gere o tipo real com:
 *
 *   npx supabase login
 *   npx supabase link --project-ref mgfpqnamjknnttdhimtn
 *   npm run db:types
 *
 * Isso substitui este arquivo por um `Database` 100% fiel ao schema (incluindo
 * todas as tabelas, views e enums), o que dá autocomplete e checagem de tipos
 * completos em todas as queries do Supabase Client.
 *
 * Por enquanto, o tipo abaixo é intencionalmente permissivo (`any`) para não
 * travar o desenvolvimento das camadas de UI e services antes da geração real.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
